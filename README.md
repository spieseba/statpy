# statpy
Lattice Charm version of statpy

### Installation
To build and install the wheel file, run the following command:
```
./build_install_wheel.sh
```
If you want to modify the code without rebuilding the software manually each time, you can create an editable installation using:
```
./editable_install.sh
```
